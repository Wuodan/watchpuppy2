name: Filesystem Proof Self-Hosted Linux

"on":
  workflow_dispatch:

permissions:
  contents: read

jobs:
  linux-cifs-share-placeholder:
    if: false
    runs-on:
      - self-hosted
      - linux
      - filesystem-proof
    steps:
      - name: Placeholder
        run: |
          echo "Self-hosted Linux proof workflow is currently an unused scaffold."
