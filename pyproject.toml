[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[project]
name = "watchpuppy2"
version = "0.0.0"

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = [
  "E", "F",
  "I",
  "UP",
  "B",
  "PL",
  "SLF",
]
per-file-ignores = { "tests/**" = ["SLF001"] }
